# 📰 News Digest Bot

Персональный Telegram-бот для получения новостного дайджеста с AI-суммаризацией.

## Как работает

1. Бот ищет новости через DuckDuckGo по выбранным темам
2. Парсит полные статьи с найденных сайтов
3. Отправляет всё в DeepSeek для анализа, фильтрации и суммаризации
4. Возвращает структурированный дайджест без воды

## Установка

```bash
# 1. Клонируй/скопируй проект
cd news_bot

# 2. Установи зависимости
pip install -r requirements.txt

# 3. Отредактируй config.py — впиши свои ключи:
#    BOT_TOKEN = "токен от @BotFather"
#    DEEPSEEK_API_KEY = "ключ от DeepSeek API"

# 4. Запусти
python bot.py
```

## Настройка

В боте нажми **⚙️ Настройки**:

- **📋 Темы** — выбери из 20 предустановленных (геополитика, IT, AI, наука...)
- **➕ Мои темы** — добавь что угодно текстом (до 20 штук)
- **📖 Уровень языка** — простой / средний / продвинутый / экспертный
- **⏱ Время чтения** — 3 / 5 / 7 / 10 / 15 минут (по умолчанию 7)
- **🌐 Язык** — русский или английский

## Использование

- **📰 Получить новости** — полный дайджест по всем твоим темам
- **🔥 Только важное** — топ новостей дня с выбором уровня фильтрации

## Стоимость

- DuckDuckGo — бесплатно
- DeepSeek API — ~$0.01-0.03 за один дайджест (~$1/мес при 2 запросах в день)

## Структура проекта

```
news_bot/
├── bot.py           # Главный файл бота (хэндлеры, клавиатуры)
├── config.py        # Конфигурация (ключи, темы, настройки)
├── database.py      # Работа с SQLite
├── news_engine.py   # Поиск, парсинг, суммаризация
├── requirements.txt # Зависимости
└── data/
    └── bot.db       # БД (создаётся автоматически)
```
